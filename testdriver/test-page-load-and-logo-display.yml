version: 4.0.78
steps:
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: the webpage displays "Basic Info"
  - prompt: >-
      Check that the logo is visible on the webpage.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: the webpage displays "stacked"
